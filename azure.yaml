name: shadcn-fastapi-starter
metadata:
  template: shadcn-fastapi-starter@0.0.1-beta

infra:
  provider: bicep
  path: infra

services:
  api:
    project: ./backend
    language: python
    host: containerapp
